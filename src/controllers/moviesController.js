const db = require('../database/models')

module.exports = {
    list : (req,res) => {

    },
    new : (req,res) => {

    },
    recommended : (req,res) => {

    },
    detail : (req,res) => {
        
    }
}
